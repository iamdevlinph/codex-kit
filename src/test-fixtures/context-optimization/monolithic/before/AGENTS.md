# Project Instructions

Follow the existing architecture. Preserve unrelated changes. Never run production deletion without explicit authorization.

## UI

Use the existing design tokens for spacing and color. Match nearby page layouts, dialog behavior, responsive breakpoints, focus management, empty states, and loading states. Check keyboard operation and accessible names. Use the existing design tokens for spacing and color.

## Database

Create additive migrations before destructive migrations. Inspect schema ownership, generate the migration with the repository command, review SQL, validate rollback behavior. Never apply production migrations without explicit authorization.

## Deployment

Run the deployment checklist only when release or production work is requested. Verify the artifact, environment, migrations, health check, and rollback plan.

## Repeated guidance

Use the existing design tokens for spacing and color. Match nearby page layouts, dialog behavior, responsive breakpoints, focus management, empty states, and loading states. Check keyboard operation and accessible names.

Create additive migrations before destructive migrations. Inspect schema ownership, generate the migration with the repository command, review SQL, validate rollback behavior. Never apply production migrations without explicit authorization.

Run the deployment checklist only when release or production work is requested. Verify the artifact, environment, migrations, health check, and rollback plan.
