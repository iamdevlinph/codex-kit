interface:
  display_name: "Audit Codex Agent Guidance"
  short_description: "Audit project instructions without syncing templates"
  default_prompt: "Use $codex-kit-audit-agents to audit this project's agent guidance."
policy:
  allow_implicit_invocation: false
